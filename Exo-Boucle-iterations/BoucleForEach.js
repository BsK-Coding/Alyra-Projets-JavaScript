// Boucle ForEach - Algorithme d'affichage des éléments du Tableau "tab" ligne par ligne

let tab = ['map', 'filter', 'forEach', 'reduce', 'every']

  ; tab.forEach((elem) => {
    console.log(elem)
  })

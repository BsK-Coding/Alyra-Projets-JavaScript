// Boucle For - Algorithme d'affichage des éléments du Tableau "tab" ligne par ligne

let tab = ['Alice', 'Bob', 'Craig', 'Dan', 'Eve']

for (let i = 0; i < tab.length; ++i) {
  console.log(`Elément ${i} du Tableau 'tab' = ${tab[i]}`)
}
/*
Ajouter au fichier précédent une fonction sub qui prend 2 paramètres et qui devra retourner la soustraction des 2 paramètres.
*/

const calcSub = (nb1, nb2) => {
  return nb1 - nb2
}

console.log(calcSub(3, 2))
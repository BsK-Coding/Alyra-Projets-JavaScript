/*
Ecrire une fonction add qui prend 2 paramètres et qui devra retourner la somme des 2 paramètres.
*/

const calcAdd = (nb1, nb2) => {
  return nb1 + nb2
}

console.log(calcAdd(2, 3))        // console.log(calcAdd(nb1, nb2))
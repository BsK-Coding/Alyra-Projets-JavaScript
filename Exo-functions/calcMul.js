/*
Ajouter au fichier précédent une fonction mul qui prend 2 paramètres et qui devra retourner la multiplication des 2 paramètres.
*/

const calcMul = (nb1, nb2) => {
  return nb1 * nb2
}

console.log(calcMul(3, 2))